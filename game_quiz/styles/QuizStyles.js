// styles/QuizStyles.js

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    // Estilos para o container do quiz, se necessário
  },
  opcoesContainer: {
    marginTop: 10,
  },
  opcao: {
    padding: 10,
    marginVertical: 5,
    backgroundColor: '#e0e0e0',
    borderRadius: 5,
  },
  opcaoSelecionada: {
    backgroundColor: '#64b5f6',
  },
});
